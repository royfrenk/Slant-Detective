# OS
.DS_Store

# Claude Code per-project runtime
.claude/

# Internal docs (not for public repo)
docs/

# Stitch mock downloads (reference material, re-fetchable via MCP)
stitch_chrome_extension_blueprint/

# Node
node_modules/
*.log

# Build output
dist/
.vite/

# Generated at build time — not committed
extension/assets/credits.json

# CWS submission zip — upload directly to developer console, don't commit
slant-detective-*.zip

# Eval harness — downloaded corpora (GPL-3.0 aggregate; re-downloadable at eval-time)
eval/data/

# Eval harness — per-run machine-generated reports (share out-of-band, not via git)
eval/reports/

# Secrets — never commit .env files (defense in depth)
.env
.env.*
!.env.example
